@using SCERP.Common
@model SCERP.Web.Areas.Inventory.Models.ViewModels.RequisitiontypeViewModel
@{
    ViewBag.Title = "Rquisitiontype";
    Layout = "~/Views/Shared/_Layout2.cshtml";
}
@using (Html.BeginForm("Index", "RequisitionType", FormMethod.Get, new { @class = "RquisitiontypeSearchForm submit-allow" }))
{
    @Html.HiddenFor(x => x.IsSearch)

    <div class="header-title">
        <h2>Store Purchase Rquisitiontype</h2>
    </div>
    <section class="common-search-section">
        <div class="search formCover common-search-wrapper">
            <table class="tabefont">
                <tr>
                    <td>
                        @Html.Label("Requisition Type:")
                    </td>
                    <td>
                        @Html.TextBoxFor(x => x.RequsitionTypeTitle, new {@data_val = "false", @placeholder = " Requisition Type"})
                    </td>
                    <td>
                        <input type="button" class="searchAction"/>
                    </td>
                </tr>
            </table>
        </div>
    </section>

}

<section class="search formCover">
    <div class="additem">
        <div class="addButton-wrapper">
            <button class="AjaxPopup addButton" action="/Inventory/RequisitionType/Edit">Add New</button>
        </div>
        @{
            var grid = new WebGrid(canPage: true, rowsPerPage: AppConfig.PageSize, canSort: true, ajaxUpdateContainerId: "grid");
            grid.Bind(Model.InventoryRequsitionTypes, rowCount: Model.TotalRecords, autoSortAndPage: false);
            grid.Pager(WebGridPagerModes.All);
            @grid.GetHtml(htmlAttributes: new { id = "grid" },
            tableStyle: "webgrid-table webgrid",
            headerStyle: "webgrid-header",
            footerStyle: "webgrid-footer",
            alternatingRowStyle: "webgrid-alternating-row",
            selectedRowStyle: "webgrid-selected-row",
            rowStyle: "webgrid-row-style",
            firstText: "First",
            lastText: "Last",
            nextText: "Next",
            mode: WebGridPagerModes.All,
            previousText: "Previous",
            columns:
            grid.Columns(
            grid.Column("Title", header: "Rquisitiontype"),
            grid.Column("Description", header: "Description"),
            grid.Column("Detail/Edit",canSort:false,format:@<text><button class="edit" action="/Inventory/RequisitionType/Edit?RequisitionTypeId=@item.RequisitionTypeId"></button></text>),
            grid.Column("Delete",canSort:false,format:@<text><button class="delete" action="/Inventory/RequisitionType/Delete?RequisitionTypeId=@item.RequisitionTypeId"></button></text>)));
        }
    </div>
</section>

