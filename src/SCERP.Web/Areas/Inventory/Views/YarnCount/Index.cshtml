@using SCERP.Common
@model SCERP.Web.Areas.Merchandising.Models.ViewModel.OmSizeViewModel
@{
    ViewBag.Title = "YarnCount";
    Layout = "~/Views/Shared/_Layout2.cshtml";
}

<div class="header-title">
    <h2>Yarn Count</h2>
</div>
<section class="common-search-section">
    <div class="search formCover common-search-wrapper">
        
        @using (Html.BeginForm("Index", "YarnCount", FormMethod.Get, new {@class = "OmSizeSearchForm"}))
        {
            @Html.HiddenFor(x => x.IsSearch)

            <table class="tabefont">
                <tr>
                    <td>
                        @Html.Label("Criteria")
                    </td>
                    <td>
                        @Html.TextBoxFor(x => x.SearchString, new {@data_val = "false", @placeholder = "Search key"})
                    </td>
                    <td>
                        <input type="button" class="searchAction"/>
                    </td>
                </tr>
            </table>
        }

    </div>
</section>

<section class="search formCover">
    <div class="additem">
        <div class="addButton-wrapper">
            <button class="AjaxPopup addButton" action="/Inventory/YarnCount/Edit">Add New</button>
        </div>
        @{
            var grid = new WebGrid(canPage: true, rowsPerPage: AppConfig.PageSize, canSort: true, ajaxUpdateContainerId: "grid");
            grid.Bind(Model.Sizes, rowCount: Model.TotalRecords, autoSortAndPage: false);
            grid.Pager(WebGridPagerModes.All);
            @grid.GetHtml(htmlAttributes: new { id = "grid" },
                tableStyle: "webgrid-table webgrid",
                headerStyle: "webgrid-header",
                footerStyle: "webgrid-footer",
                alternatingRowStyle: "webgrid-alternating-row",
                selectedRowStyle: "webgrid-selected-row",
                rowStyle: "webgrid-row-style",
                firstText: "First",
                lastText: "Last",
                nextText: "Next",
                mode: WebGridPagerModes.All,
                previousText: "Previous",
                columns:
                    grid.Columns(
                        grid.Column("SizeRefId", header: "Ref No"),
                        grid.Column("SizeName", header: "Yarn"),
                        grid.Column("Detail/Edit", canSort: false, format: @<text><button class="edit" action="/Inventory/YarnCount/Edit?SizeId=@item.SizeId"></button></text>),
                        grid.Column("Delete", canSort: false, format:@<text><button class="delete" action="/Inventory/YarnCount/Delete?SizeRefId=@item.SizeRefId"></button></text>)));
        }
    </div>
</section>

