@using SCERP.Common
@model SCERP.Web.Areas.Production.Models.BatchViewModel
@{
    ViewBag.Title = "DailyDyeingBatch";
    Layout = "~/Views/Shared/_InventoryLayout.cshtml";
}
@using (Html.BeginForm("DailyDyeingBatch", "Batch", FormMethod.Get, new { @class = "BatchForm ignore-validation" }))
{
    <div class="header-title">
        <h2>Daily Dyeing Batch Information</h2>
    </div>
    <section class="common-search-section">
        <div class="search formCover common-search-wrapper">
            <table class="tabefont">
                <tr>
                    <td>@Html.Label("Batch Date")</td>
                    <td>
                        @Html.TextBoxFor(x => x.BatchDate, new {@class = "datepicker"})
                    </td>

                    <td>
                        @Html.Label("Status")
                    </td>
                    <td>
                        @Html.DropDownListFor(x => x.BatchStatus, Model.BatchStatuSelectListItem, "-Select-")
                    </td>
                    <td>
                        @Html.Label("*", new {@class = "labelInstruction"})
                    </td>

                    <td>
                        @Html.Label("Batch Type ")
                    </td>
                    <td>
                        @Html.DropDownListFor(x => x.BtType, Model.BatchTypeSelectListItem)
                    </td>
                    <td>
                        @Html.Label("*", new {@class = "labelInstruction"})
                    </td>

                    <td>
                        <input type="button" class="searchAction"/>
                    </td>
                </tr>
            </table>
        </div>
    </section>
}
<section class="search formCover">
    <div class="additem">
        @{
            var grid = new WebGrid(canPage: true, rowsPerPage: AppConfig.PageSize, canSort: true, ajaxUpdateContainerId: "grid");
            grid.Bind(Model.VProBatches, rowCount: Model.TotalRecords, autoSortAndPage: false);
            grid.Pager(WebGridPagerModes.All);
            @grid.GetHtml(htmlAttributes: new { id = "grid" },
            tableStyle: "webgrid-table webgrid",
            headerStyle: "webgrid-header",
            footerStyle: "webgrid-footer",
            alternatingRowStyle: "webgrid-alternating-row",
            selectedRowStyle: "webgrid-selected-row",
            rowStyle: "webgrid-row-style",
            firstText: "First",
            lastText: "Last",
            nextText: "Next",
            mode: WebGridPagerModes.All,
            previousText: "Previous",
            columns:
            grid.Columns(
            grid.Column("BtRefNo", header: "Ref No"),
            grid.Column("BatchNo", header: "Batch No"),
            grid.Column("BatchDate", header: "Date", format: @<text>@(item.BatchDate == null ? "" : item.BatchDate.ToString("dd-MM-yyyy"))</text>),
            grid.Column("PartyName", header: "Party "),
            grid.Column("MachineName", header: "Machine"),
                    grid.Column("GColorName", header: "Color"),
            grid.Column("BatchQty", header: "Qty"),
            grid.Column("ShadePerc", header: "Shade %"),
            grid.Column("CostRate", header: "Cost Rate"),
            grid.Column("BillRate", header: "BillRate"),
            grid.Column(header: "Status", format: (item) =>(BatchStatus)item.BatchStatus),
            grid.Column("Report", canSort: false, header: "BATCH", format: @<text><a href="/Production/ProductionReport/GetBatchDetail?BatchId=@item.BatchId" target="_blank">PDF</a></text>)));
        }
    </div>
</section>

