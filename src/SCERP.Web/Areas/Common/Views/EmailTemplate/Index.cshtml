@using SCERP.Common
@model SCERP.Web.Areas.Common.Models.ViewModel.EmailTemplateViewModel
@{
    ViewBag.Title = "Mail User";
    Layout = "~/Views/Shared/_InventoryLayout.cshtml";
}
<div class="header-title">
    <h2>Email Tamplate Information</h2>
</div>
<section class="common-search-section">
    <div class="search formCover common-search-wrapper">

        @using (Html.BeginForm("Index", "EmailTemplate", FormMethod.Get, new { @class = "EmailTemplateSearchForm" }))
        {
            @Html.HiddenFor(x => x.IsSearch)
            <table class="tabefont">
                <tr>
                    <td>
                        @Html.Label("Criteria")
                    </td>
                    <td>
                        @Html.TextBoxFor(x => x.SearchString, new { @data_val = "false", @placeholder = "Search key" })
                    </td>
                    <td>
                        <input type="button" class="searchAction" />
                    </td>
                </tr>
            </table>
        }

    </div>
</section>

<section class="search formCover">
    
    <div class="additem">
        <div class="addButton-wrapper">
            <button class="AjaxPopup addButton" action="/Common/EmailTemplate/Edit">Add New</button>
        </div>
        @{
            var grid = new WebGrid(canPage: true, rowsPerPage: AppConfig.PageSize, canSort: true, ajaxUpdateContainerId: "grid");
            grid.Bind(Model.EmailTemplates, rowCount: Model.TotalRecords, autoSortAndPage: false);
            grid.Pager(WebGridPagerModes.All);
            @grid.GetHtml(htmlAttributes: new {id = "grid"},
                tableStyle: "webgrid-table webgrid",
                headerStyle: "webgrid-header",
                footerStyle: "webgrid-footer",
                alternatingRowStyle: "webgrid-alternating-row",
                selectedRowStyle: "webgrid-selected-row",
                rowStyle: "webgrid-row-style",
                firstText: "First",
                lastText: "Last",
                nextText: "Next",
                mode: WebGridPagerModes.All,
                previousText: "Previous",
                columns:
                    grid.Columns(
                       grid.Column("EmailTemplateRefId", header: "ID"),
                        grid.Column("EmailTemplateName", header: "EmailTemplateName"),
                     
                        grid.Column("Detail/Edit", canSort: false, format: @<text><button class="edit" action="/Common/EmailTemplate/Edit?EmailTemplate.EmailTemplateId=@item.EmailTemplateId"></button></text>),
                        grid.Column("Delete", canSort: false, format:@<text><button class="delete" action="/Common/EmailTemplate/Delete?EmailTemplate.EmailTemplateId=@item.EmailTemplateId"></button></text>)))
            ;
        }
    </div>
</section>

