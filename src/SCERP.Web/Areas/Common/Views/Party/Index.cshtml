@using SCERP.Common
@model SCERP.Web.Areas.Common.Models.ViewModel.PartyViewModel
@{
    ViewBag.Title = "Party";
    Layout = "~/Views/Shared/_Layout2.cshtml";
}
<div class="header-title">
    <h2>Party Information</h2>
</div>
<section class="common-search-section">
    <div class="search formCover common-search-wrapper">

        @using (Html.BeginForm("Index", "Party", FormMethod.Get, new { @class = "PartySearchForm" }))
        {
            @Html.HiddenFor(x => x.IsSearch)
           
                <table class="tabefont">
                    <tr>
                        <td>
                            @Html.Label("Criteria")
                        </td>
                        <td>
                            @Html.TextBoxFor(x => x.Name, new { @data_val = "false", @placeholder = "Search key" })
                        </td>
                        <td>
                            <input type="button" class="searchAction" />
                        </td>
                    </tr>
                </table>
           
        }
    </div>
</section>

<section class="search formCover">
    <div class="additem">
        <div style="margin-bottom: 3px;">
            <button class="AjaxPopup addButton" action="/Common/Party/Edit">Add New</button>
        </div>
        @{
            var grid = new WebGrid(canPage: true, rowsPerPage: AppConfig.PageSize, canSort: true, ajaxUpdateContainerId: "grid");
            grid.Bind(Model.Parties, rowCount: Model.TotalRecords, autoSortAndPage: false);
            grid.Pager(WebGridPagerModes.All);
            @grid.GetHtml(htmlAttributes: new { id = "grid" },
            tableStyle: "webgrid-table webgrid",
            headerStyle: "webgrid-header",
            footerStyle: "webgrid-footer",
            alternatingRowStyle: "webgrid-alternating-row",
            selectedRowStyle: "webgrid-selected-row",
            rowStyle: "webgrid-row-style",
            firstText: "First",
            lastText: "Last",
            nextText: "Next",
            mode: WebGridPagerModes.All,
            previousText: "Previous",
            columns:
            grid.Columns(
            grid.Column("PartyRefNo", header: "Ref No"),
            grid.Column("Name", header: "Name"),
            grid.Column("Address", header: "Address"),
            grid.Column("Email", header: "Email"),
            grid.Column("Phone", header: "Phone"),
            grid.Column("ContactPersonName", header: "ContactPerson"),
             grid.Column("ContactPhone", header: "Contact Phone"),
            grid.Column("Detail/Edit", canSort: false, format: @<text><button class="edit" action="/Common/Party/Edit?PartyId=@item.PartyId"></button></text>),
            grid.Column("Delete",canSort:false,format:@<text><button class="delete" action="/Common/Party/Delete?PartyId=@item.PartyId"></button></text>)));
        }
    </div>
</section>

