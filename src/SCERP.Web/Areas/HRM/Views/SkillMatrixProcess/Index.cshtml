@using SCERP.Common
@model SCERP.Web.Areas.HRM.Models.ViewModels.SkillMatrixProcessViewModel
@{ 
    ViewBag.Title = "SkillMatrixProcess";
    Layout = "~/Views/Shared/_Layout2.cshtml";
}

<div class="header-title">
    <h2>Skill Matrix Process List</h2>
</div>
<section class="common-search-section">
    <div class="search formCover common-search-wrapper">
        
        @using (Html.BeginForm("Index", "SkillMatrixProcess", FormMethod.Get, new {@class = "SkillMatrixProcessForm submit-allow"}))
        {
            @Html.HiddenFor(x => x.IsSearch)

            <table>
                <tr>
                    <td>
                        @Html.Label("Criteria")
                    </td>
                    <td>
                        @Html.TextBoxFor(x => x.SearchString, new {@placeholder = "Search key"})
                    </td>
                    <td>
                        <input type="button" class="searchAction"/>
                    </td>
                </tr>
            </table>
        }

    </div>
</section>

<section class="search formCover">
    <div class="additem">
        <div class="addButton-wrapper">
            <button class="AjaxPopup addButton" action="/HRM/SkillMatrixProcess/Edit">Add New</button>
        </div>
        @{
            var grid = new WebGrid(canPage: true, rowsPerPage: AppConfig.PageSize, canSort: true, ajaxUpdateContainerId: "grid");
            grid.Bind(Model.SkillMatrixProcesses, rowCount: Model.TotalRecords, autoSortAndPage: false);
            grid.Pager(WebGridPagerModes.All);
            @grid.GetHtml(htmlAttributes: new { id = "grid" },
                tableStyle: "webgrid-table webgrid",
                headerStyle: "webgrid-header",
                footerStyle: "webgrid-footer",
                alternatingRowStyle: "webgrid-alternating-row",
                selectedRowStyle: "webgrid-selected-row",
                rowStyle: "webgrid-row-style",
                firstText: "First",
                lastText: "Last",
                nextText: "Next",
                mode: WebGridPagerModes.All,
                previousText: "Previous",
                columns:
                  grid.Columns(
                  grid.Column("ProcessName", header: "ProcessName"),
                  grid.Column("DisplayOrder", header: "DisplayOrder"),
                  grid.Column("Edit", canSort: false, format: @<text><button class="edit" action="/HRM/SkillMatrixProcess/Edit?SkillMatrixProcess.SkillMatrixProcessId=@item.SkillMatrixProcessId"></button></text>),
                  grid.Column("Delete", canSort: false, format:@<text><button class="delete" action="/HRM/SkillMatrixProcess/Delete?SkillMatrixProcessId=@item.SkillMatrixProcessId"></button></text>)));
        }
    </div>
</section>









